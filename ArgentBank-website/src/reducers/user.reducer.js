const initialState = {};

export default function userReducer(state = initialState, action) {
  return state;
  // switch
  // switch (action.type) {
  //   case value:

  //     break;

  //   default:
  //     break;
}
